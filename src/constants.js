export const DB_NAME ="bddata"  // Database name
// export const DB_URL = `mongodb://localhost:27017/${DB_NAME}`;  // MongoDB connection URL
// export const PORT = process.env.PORT || 5000;  // Server port, default to 5000 if not set in environment variables
// export const CORS_ORIGIN = process.env.CORS_ORIGIN || "http://localhost:3000";  // CORS origin, default to localhost:3000 if not set in environment variables